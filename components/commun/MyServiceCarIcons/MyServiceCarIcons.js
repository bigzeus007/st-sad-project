import React from "react";
import MyIcons from "../images/images";
import styled from "styled-components";
import Doing from "../images/Doing.png"

export default function MyServiceCarIcons(carTasks) {
  const IntervetionsIcons = styled.img`

    position: absolute;
    
    
    top: -20px;
    left: 20px;
    
  `;   

//   const {myService} = carTasks;
//   const {mecanical} = carTasks;
//   const {electrical} = carTasks;
//   const {body} = carTasks;
//   const {divers} = carTasks;

  return (
    <IntervetionsIcons ></IntervetionsIcons>
  );
}
