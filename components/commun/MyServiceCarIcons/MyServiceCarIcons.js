import React from "react";
import MyIcons from "../../../src/images";
import styled from "styled-components";

export default function MyServiceCarIcons(carTasks) {
  const IntervetionsIcons = styled.img`
    position: absolute;

    top: -20px;
    left: 20px;
  `;

  //   const {express} = carTasks;
  //   const {mecanique} = carTasks;
  //   const {diagnostic} = carTasks;
  //   const {body} = carTasks;
  //   const {divers} = carTasks;

  return <IntervetionsIcons></IntervetionsIcons>;
}
